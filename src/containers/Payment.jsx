import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Payment extends Component {
  render() {
    return (
      <div class="Payment">
        <h1 class="Payment-title">Payment tests</h1>
      </div>
    );
  }
};

export default (Payment)